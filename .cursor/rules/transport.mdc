---
description: PHP SDK HTTP transport and batch queue rules
globs:
  - "packages/talaria/src/Transport/**"
  - "packages/talaria/tests/**"
---

# Transport & queue

## Wire contract

Events:

- `POST {baseUrl}/events/ingestBatch`
- Header: `X-API-Key` only (no dual Authorization unless needed)
- Body:

```json
{
  "input": {
    "__className__": "IngestEventBatchInput",
    "events": [
      { "__className__": "IngestEventInput", "message": "...", "environment": "production" }
    ]
  }
}
```

Spans (when tracing is enabled):

- `POST {baseUrl}/spans/ingestBatch`
- Same `X-API-Key`
- Body: `{ "input": { "__className__": "IngestSpanBatchInput", "spans": [ { "__className__": "IngestSpanInput", ... } ] } }`
- `SpanTransport` constructs its **own** Guzzle client. Do not install `GuzzleMiddleware` on ingest transports.

- Even a single leftover event or span is a batch of one
- `extra` → `extraJson` (JSON **string**); `tags` are string→string
- Flat `userId` / `url` — not nested `user` / `request`
- First-class `exception` (`ExceptionDataDto` / frames with `functionName`) — never put `exception_class` / `file` / `line` / `code` in `extra`
- `platform: "php"` on all PHP captures (exceptions and messages); keep raw `stackTrace` string alongside structured frames

## EventQueue drain triggers

| Trigger | Config | When |
| ------- | ------ | ---- |
| Size | `maxBatchSize` (default 50) | Buffer reaches N |
| Age | `flushIntervalMs` (default 2000) | Checked on each enqueue |
| Explicit | `flush()` / `close()` | Always drain |
| Shutdown | `register_shutdown_function` | Drain remainder |

PHP FPM has no background timer — age is checked on enqueue, not via a wall-clock thread.

## Reliability

- Short HTTP timeout (2–3s)
- No unbounded retries; on 429 log warning and drop/backoff once
- Transport failures must not crash the host app
- Call `fastcgi_finish_request()` when available before shutdown network I/O
- On ingest `retry: false` (or known permanent class names when `retry` is absent): drop the batch, log once, and disable events **and** spans for the rest of this PHP process. Quota / rate-limit (`retry: true`) and 5xx must **not** disable. FPM workers reuse the singleton — a new worker after deploy picks up a rotated key.
